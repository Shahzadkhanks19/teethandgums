"use client";

/**
 * Disabled intentionally for performance.
 * The full-screen animated route overlay loaded Framer Motion and an image
 * for every page, while NavigationProgress still provides lightweight feedback.
 */
export default function NavigationOverlay() {
  return null;
}
